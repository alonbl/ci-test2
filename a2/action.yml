name: 'a2'
description: 'a2'
runs:
  using: "composite"
  steps:
    - shell: sh
      run:
        echo a2
    - shell: sh
      run: |
        . "${{ github.action_path }}/../.action"
        if ! [ -d "${action_repo}" ]; then
          ln -sfn "${{ github.action_path }}/.." "${action_repo}"
        fi
    - uses: ./ci-test2/a3
